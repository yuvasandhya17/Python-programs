n=int(input("enter a num"))
l=list(map(int,input().split()))
for i in l:
    count=0
    for j in l:
        if j%2==0:
            count=count+1
    if count==1:
         print("the even count is ",count,j ,"is the number")
